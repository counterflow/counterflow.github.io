# Copy this file to `.env` and fill in the value.
# Used by src/components/ContactForm.astro (read at build time).
# For deployed builds, add the same value as a GitHub Actions secret.
WEB3FORMS_ACCESS_KEY=
